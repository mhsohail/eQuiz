@model eQuiz.ViewModels.QuestionViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>
<script>window.onload = window.history.forward(0);</script>
<div>
    <h4>Question</h4>
    <hr />
    <p style="color:red" class="col-md-offset-1 col-md-11">@((Model.IsLastQuestion) ? "This is last question." : "")</p>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Question.Text):
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Question.Text)
        </dd>
    </dl>
    
    @using (Html.BeginForm("Details", "Questions", new { id = Model.NextId }))
    {
        <div class="form-horizontal">
            @foreach (var Answer in Model.Answers)
            {
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10 col-sm-offset-2 col-sm-10">
                        @Html.RadioButtonFor(model => Answer.Text, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.DisplayFor(model => Answer.Text, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => Answer.Text, "", new { @class = "text-danger" })
                    </div>
                </div>
            }

            <div class="form-group">
                @*<div class="col-md-offset-4 col-md-8 col-sm-offset-4 col-sm-8">*@
                <div class="col-md-offset-2 col-md-10 col-sm-offset-2 col-sm-10">
                    <input type="submit" value="Next" class="btn btn-default" style="width:150px;" @((Model.IsLastQuestion) ? "disabled" : "") />
                </div>
            </div>
        </div>
    }
</div>
@*<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Question.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>*@
